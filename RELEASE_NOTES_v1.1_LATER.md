# v1.1 - Just Better

Fixed the annoying stuff. Cleaned up the mess. Works like it should.

- Better logging (you'll actually understand what's happening)
- Less duplicate code (because copy-paste is lazy)
- Updated dependencies (security stuff)
- Proper signal handling (no more weird hangs)

Same simple setup. Same reliable service. Just... better.

That's it. No magic. Just code that doesn't suck as much.